"use client";
export const dynamic = "force-dynamic";
export const fetchCache = "force-no-store";

import ProtectedDirectoryPage from "@/components/ProtectedDirectoryPage";

export default ProtectedDirectoryPage;
