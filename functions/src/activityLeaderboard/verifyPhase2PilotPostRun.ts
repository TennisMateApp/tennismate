/* eslint-disable max-len, no-console, require-jsdoc, curly, brace-style, block-spacing */
import * as admin from "firebase-admin";
import {promises as fs} from "fs";
import path from "path";
import {calculateMonthlyActivity} from "./monthlyCalculation";
import {storedActivityEvent} from "./persistence";

const PROJECT_ID = "tennismate-d8acb";
const MONTH = "2026-06";
const EXPECTED_GENERATION = "v1-70ffe0d27bb85de3c7b2";
const EXPECTED_CHECKSUM = "70ffe0d27bb85de3c7b25ad3c56e180f0c21119cd8ea076b3b9f3d345c2a629a";
const RANKING_FIELDS = ["avatarUrl", "displayName", "distinctOpponentCount", "eligibleActivityCount", "playerId", "points", "rank", "scoringActivityCount"].sort();
const FORBIDDEN_FIELDS = ["postcode", "lat", "lng", "email", "birthYear", "birthDate", "availability", "skill", "skillBand", "skillRating", "utr", "bio", "geohash", "opponentIds", "sourcePath"];

function iso(value: unknown): string | null {
  if (value && typeof (value as {toDate?: () => Date}).toDate === "function") return (value as {toDate: () => Date}).toDate().toISOString();
  return null;
}
function expectedProfile(data: FirebaseFirestore.DocumentData): {displayName: string | null; avatarUrl: string | null} {
  return {displayName: typeof data.name === "string" && data.name.trim() ? data.name.trim().slice(0, 80) : null, avatarUrl: typeof data.photoThumbURL === "string" ? data.photoThumbURL : typeof data.photoURL === "string" ? data.photoURL : typeof data.avatar === "string" ? data.avatar : null};
}

async function main(): Promise<void> {
  if (process.env.FIRESTORE_EMULATOR_HOST || process.env.FUNCTIONS_EMULATOR) throw new Error("Production post-run verification refuses emulator environments");
  if (admin.apps.length === 0) admin.initializeApp({projectId: PROJECT_ID}); const db = admin.firestore();
  const roots = await db.listCollections(); const activityCollections = roots.map((collection) => collection.id).filter((id) => id.startsWith("activity_")).sort(); const runCollections = activityCollections.filter((id) => /phase2|pilot|recalculation_run|leaderboard_run/i.test(id));
  const [requests, monthParentSnapshot, leaderboardParentSnapshot, eventSnapshot, aggregateRootSnapshot, leaderboardRootSnapshot] = await Promise.all([db.collection("activity_recalculation_requests").get(), db.doc(`activity_months/${MONTH}`).get(), db.doc(`activity_leaderboards/${MONTH}`).get(), db.collection("activity_match_events").where("monthKey", "==", MONTH).get(), db.collection("activity_months").get(), db.collection("activity_leaderboards").get()]);
  const monthParent = monthParentSnapshot.data() || {}; const leaderboardParent = leaderboardParentSnapshot.data() || {}; const publishedGenerationId = leaderboardParent.publishedGenerationId || null; const monthGenerationId = monthParent.publishedGenerationId || null;
  const [monthGenerations, leaderboardGenerations] = await Promise.all([db.collection(`activity_months/${MONTH}/generations`).get(), db.collection(`activity_leaderboards/${MONTH}/generations`).get()]);
  const aggregateSnapshot = publishedGenerationId ? await db.collection(`activity_months/${MONTH}/generations/${publishedGenerationId}/players`).get() : null; const rankingSnapshot = publishedGenerationId ? await db.collection(`activity_leaderboards/${MONTH}/generations/${publishedGenerationId}/rankings`).get() : null;
  const events = eventSnapshot.docs.map((document) => storedActivityEvent(document.data())); const calculation = calculateMonthlyActivity(events, MONTH); const calculatedByPlayer = new Map(calculation.aggregates.map((aggregate) => [aggregate.playerId, aggregate]));
  const playerRefs = (rankingSnapshot?.docs || []).map((document) => db.doc(`players/${document.id}`)); const profiles = playerRefs.length ? await db.getAll(...playerRefs) : []; const profileById = new Map(profiles.map((profile) => [profile.id, expectedProfile(profile.data() || {})]));
  const aggregateChecks = (aggregateSnapshot?.docs || []).map((document) => {const data = document.data(); const expected = calculatedByPlayer.get(document.id); return {path: document.ref.path, eligibleActivityCount: data.eligibleActivityCount, scoringActivityCount: data.cappedActivityCount, distinctOpponentCount: data.distinctOpponentCount, points: data.activityPoints, metricsMatch: Boolean(expected && data.eligibleActivityCount === 1 && data.cappedActivityCount === 1 && data.distinctOpponentCount === 1 && data.activityPoints === 15), forbiddenFieldsPresent: FORBIDDEN_FIELDS.filter((field) => field in data)};});
  const rankingChecks = (rankingSnapshot?.docs || []).map((document) => {const data = document.data(); const expected = profileById.get(document.id); return {path: document.ref.path, rank: data.rank, points: data.points, eligibleActivityCount: data.eligibleActivityCount, scoringActivityCount: data.scoringActivityCount, distinctOpponentCount: data.distinctOpponentCount, metricsMatch: data.rank === 1 && data.points === 15 && data.eligibleActivityCount === 1 && data.scoringActivityCount === 1 && data.distinctOpponentCount === 1, profileSnapshotMatchesCurrentPublicProfile: Boolean(expected && data.displayName === expected.displayName && data.avatarUrl === expected.avatarUrl), exactApprovedFieldSet: JSON.stringify(Object.keys(data).sort()) === JSON.stringify(RANKING_FIELDS), forbiddenFieldsPresent: FORBIDDEN_FIELDS.filter((field) => field in data)};});
  const requestChecks = requests.docs.map((document) => ({path: document.ref.path, monthKey: document.id, status: document.data().status || null, attempts: document.data().attempts ?? 0, startedAt: iso(document.data().startedAt), completedAt: iso(document.data().completedAt), publishedGenerationId: document.data().publishedGenerationId || null, checksum: document.data().actualChecksum || null, aggregateCount: document.data().aggregateCount ?? null, rankingCount: document.data().rankingCount ?? null, errorCategory: document.data().errorCategory ?? null})).sort((a, b) => a.monthKey.localeCompare(b.monthKey)); const juneRequest = requestChecks.find((request) => request.monthKey === MONTH) || null;
  const currentGenerationMetadata = leaderboardGenerations.docs.find((document) => document.id === publishedGenerationId)?.data() || {}; const retiredGenerations = leaderboardGenerations.docs.filter((document) => document.id !== publishedGenerationId).map((document) => ({path: document.ref.path, status: document.data().status || null, deleteAfter: iso(document.data().deleteAfter)}));
  const calculatedIds = new Set(calculation.rankings.map((ranking) => ranking.playerId)); const publishedIds = new Set((rankingSnapshot?.docs || []).map((document) => document.id)); const stalePublishedRowCount = [...publishedIds].filter((id) => !calculatedIds.has(id)).length; const missingPublishedRowCount = [...calculatedIds].filter((id) => !publishedIds.has(id)).length;
  const report = {mode: "READ_ONLY_POST_RUN_VERIFICATION", projectId: PROJECT_ID, generatedAt: new Date().toISOString(), activityCollections, pilotRunRecord: {dedicatedRunCollections: runCollections, dedicatedRunRecordFound: false, publicationRecordPath: publishedGenerationId ? `activity_leaderboards/${MONTH}/generations/${publishedGenerationId}` : null, lifecycleRecordPath: `activity_recalculation_requests/${MONTH}`, runId: currentGenerationMetadata.runId || leaderboardParent.runId || null, storedStatus: currentGenerationMetadata.status || null, startedAt: juneRequest?.startedAt || null, completedAt: juneRequest?.completedAt || null, generationId: publishedGenerationId, sourceChecksum: currentGenerationMetadata.sourceChecksum || null, createdAggregateRowsInferredFromEmptyPreRunState: aggregateSnapshot?.size ?? 0, createdRankingRowsInferredFromEmptyPreRunState: rankingSnapshot?.size ?? 0, updatedGeneratedRowsInferredFromEmptyPreRunState: 0, errorCategory: juneRequest?.errorCategory || null}, requests: {june: juneRequest, all: requestChecks, otherPendingCount: requestChecks.filter((request) => request.monthKey !== MONTH && request.status === "pending").length, otherNonPendingCount: requestChecks.filter((request) => request.monthKey !== MONTH && request.status !== "pending").length}, aggregates: {parentPath: `activity_months/${MONTH}`, generationMetadataPath: `activity_months/${MONTH}/generations/${publishedGenerationId}`, count: aggregateSnapshot?.size ?? 0, documents: aggregateChecks}, leaderboard: {parentPath: `activity_leaderboards/${MONTH}`, generationMetadataPath: `activity_leaderboards/${MONTH}/generations/${publishedGenerationId}`, count: rankingSnapshot?.size ?? 0, documents: rankingChecks}, publication: {monthPublishedGenerationId: monthGenerationId, leaderboardPublishedGenerationId: publishedGenerationId, expectedGenerationId: EXPECTED_GENERATION, generationMatches: monthGenerationId === EXPECTED_GENERATION && publishedGenerationId === EXPECTED_GENERATION, monthGenerationCount: monthGenerations.size, leaderboardGenerationCount: leaderboardGenerations.size, retiredGenerations, stalePublishedRowCount, missingPublishedRowCount}, reconciliation: {sourceEventCount: calculation.sourceEventCount, scoringEventCount: calculation.scoringEventCount, aggregateDocumentCount: aggregateSnapshot?.size ?? 0, leaderboardDocumentCount: rankingSnapshot?.size ?? 0, totalPublishedPoints: (rankingSnapshot?.docs || []).reduce((sum, document) => sum + Number(document.data().points || 0), 0), calculatedChecksum: calculation.sourceChecksum, expectedChecksum: EXPECTED_CHECKSUM, checksumMatches: calculation.sourceChecksum === EXPECTED_CHECKSUM && currentGenerationMetadata.sourceChecksum === EXPECTED_CHECKSUM, aggregateDiscrepancyCount: aggregateChecks.filter((item) => !item.metricsMatch || item.forbiddenFieldsPresent.length).length, leaderboardDiscrepancyCount: rankingChecks.filter((item) => !item.metricsMatch || !item.profileSnapshotMatchesCurrentPublicProfile || !item.exactApprovedFieldSet || item.forbiddenFieldsPresent.length).length, stalePublishedRowCount, missingPublishedRowCount}, isolation: {aggregateMonthParentPaths: aggregateRootSnapshot.docs.map((document) => document.ref.path).sort(), leaderboardMonthParentPaths: leaderboardRootSnapshot.docs.map((document) => document.ref.path).sort()}};
  const output = path.resolve("../activity-leaderboard-phase2-june-post-run-verification.json"); await fs.writeFile(output, `${JSON.stringify(report, null, 2)}\n`, "utf8"); console.log(JSON.stringify(report, null, 2)); console.log(`Saved read-only verification: ${output}`);
}

main().catch((error) => {console.error(error instanceof Error ? error.message : "Unknown verification error"); process.exitCode = 1;});
